[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "we-telecom-agent"
version = "0.1.0"
description = "Portfolio web application for a tool-calling WE Telecom support agent."
requires-python = ">=3.11,<3.14"
dependencies = [
  "certifi>=2025.1.31",
  "cryptography>=44.0.0",
  "fastapi>=0.115.0,<1.0",
  "jinja2>=3.1.5",
  "langchain>=0.3.0,<1.0",
  "langchain-classic>=0.3.0,<2.0",
  "langchain-community>=0.3.0,<1.0",
  "langchain-google-genai>=2.0.0,<4.0",
  "langchain-huggingface>=0.1.0,<2.0",
  "langchain-mongodb>=0.6.0,<1.0",
  "langchain-qdrant>=0.2.0,<2.0",
  "langchain-text-splitters>=0.3.0,<2.0",
  "pymongo>=4.10.0,<5.0",
  "qdrant-client>=1.12.0,<2.0",
  "sentence-transformers>=3.3.0,<4.0",
  "uvicorn[standard]>=0.34.0,<1.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.3.0", "ruff>=0.9.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]

[tool.setuptools.packages.find]
include = ["app*"]
